{
  "name": "elysia",
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build src/app.js --out-dir dist --out-file bundle.js --no-source-maps && gulp obfuscate"
  },
  "devDependencies": {
    "parcel": "^2.10.0",
    "gulp": "^4.0.2",
    "gulp-javascript-obfuscator": "^3.0.0"
  }
}